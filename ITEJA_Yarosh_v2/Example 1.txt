program myProgram;

var a : double;
    name : string;
begin
    //readln(a);
    //name := "Nikita" + write("Hello, ", 10, "!");
    //writeln();
    //writeln(round(cos(PI)));
    //cos(PI);
    {for a := 1 to 10 do
    begin
        writeln(a * "*")
    end}
end.