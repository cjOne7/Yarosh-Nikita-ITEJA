program myProgram;

var a : double;
    name : string;
begin
    //name := "Nikita" + write("Hello, ", 10, "!");//error due to write can be called only as statement
    //writeln(round(cos(PI)));
    //cos(PI);
    {readln(a);
    for a := 1 to 10 do
    begin
        writeln(a * "*")
    end}
end.