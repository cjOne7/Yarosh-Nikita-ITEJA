const x : string = "const", y : double = -4.4;
var i : double, r : double, l : double, result : string;
begin
    i := 1;
    while i # 10 do
    begin
        !"*"*i;
        i:=i+1
    end;

    !"Enter radius:";
    ?r;
    l := 2 * PI * r;
    result := "Circle length: " + l + "cm.";
    !result
end.